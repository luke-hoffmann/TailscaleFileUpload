{
  "name": "taildrop-inbox",
  "version": "1.0.0",
  "private": true,
  "description": "A tiny phone-to-computer file inbox for your Tailscale network.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "check": "node --check server.mjs && node --check cleanup-worker.mjs && node --check public/app.js && node --check test-all.js",
    "test": "node test-all.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "qrcode": "^1.5.3"
  }
}
